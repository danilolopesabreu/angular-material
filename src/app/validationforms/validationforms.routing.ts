import { Routes } from '@angular/router';
import { ValidationFormsComponent } from './validationforms.component';

export const ValidationFormsRoutes: Routes = [
    {
        path: '',
        component: ValidationFormsComponent
    }
];